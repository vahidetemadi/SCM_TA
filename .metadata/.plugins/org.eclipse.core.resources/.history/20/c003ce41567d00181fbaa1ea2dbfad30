package SCM_TA_V1;

import java.io.File;
import java.io.FileNotFoundException;
import java.io.PrintWriter;
import java.util.*;

public class changeRepresentation {

	public static void txtToCSV() throws FileNotFoundException{
		
		Scanner sc;
		PrintWriter pw;
		StringBuilder sb=new StringBuilder();
		for(File fileEntry:new File(System.getProperty("user.dir")+"//src//SCM_TA_V1//bug-data//JDT//efforts").listFiles()){
			sc=new Scanner(new File(fileEntry.toURI()));
			pw=new PrintWriter(new File(System.getProperty("user.dir")+"//results//analysis.csv"));
			int i=0;
			while(sc.hasNextLine()){
				if(i%11==0){
					
				}
				else(){
					
				}
				
			}
		}
	}
}
